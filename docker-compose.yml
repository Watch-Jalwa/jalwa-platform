services:
  web:
    build:
      context: .
      target: web
    env_file: .env
    ports: ["3000:3000"]
    restart: unless-stopped

  worker:
    build:
      context: .
      target: worker
    env_file: .env
    restart: unless-stopped
