name: Bootstrap platform accounts

on:
  workflow_dispatch:
    inputs:
      digitalocean_region:
        description: DigitalOcean region slug
        required: true
        default: blr1
      supabase_region:
        description: Supabase region slug
        required: true
        default: ap-southeast-1
      admin_cidr:
        description: Trusted SSH CIDR, such as 203.0.113.10/32
        required: true
      domain:
        description: Production domain
        required: true
        default: watch-jalwa.com

permissions:
  contents: read
  actions: write

concurrency:
  group: jalwa-platform-bootstrap
  cancel-in-progress: false

jobs:
  bootstrap:
    runs-on: ubuntu-latest
    environment: production
    env:
      GH_TOKEN: ${{ github.token }}
      DIGITALOCEAN_TOKEN: ${{ secrets.DIGITALOCEAN_TOKEN }}
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      CLOUDFLARE_ZONE_ID: ${{ secrets.CLOUDFLARE_ZONE_ID }}
      AWS_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
      SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
      SUPABASE_ORG_ID: ${{ secrets.SUPABASE_ORG_ID }}
      SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
    steps:
      - uses: actions/checkout@v6
      - uses: hashicorp/setup-terraform@v3
      - uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Validate required account connections
        env:
          SSH_PUBLIC_KEY: ${{ secrets.DIGITALOCEAN_SSH_PUBLIC_KEY }}
        run: |
          set -euo pipefail
          for value in DIGITALOCEAN_TOKEN CLOUDFLARE_API_TOKEN CLOUDFLARE_ACCOUNT_ID CLOUDFLARE_ZONE_ID AWS_ACCESS_KEY_ID AWS_SECRET_ACCESS_KEY SUPABASE_ACCESS_TOKEN SUPABASE_ORG_ID SUPABASE_DB_PASSWORD SSH_PUBLIC_KEY; do
            test -n "${!value:-}" || { echo "Missing $value" >&2; exit 1; }
          done

      - name: Create Cloudflare R2 buckets
        run: |
          set -euo pipefail
          for bucket in jalwa-terraform-state jalwa-incoming jalwa-media jalwa-rights-evidence; do
            status=$(curl -sS -o /tmp/r2.json -w '%{http_code}' \
              -X POST "https://api.cloudflare.com/client/v4/accounts/$CLOUDFLARE_ACCOUNT_ID/r2/buckets" \
              -H "Authorization: Bearer $CLOUDFLARE_API_TOKEN" \
              -H 'Content-Type: application/json' \
              --data "{\"name\":\"$bucket\"}")
            if [[ "$status" != "200" && "$status" != "201" && "$status" != "409" ]]; then
              cat /tmp/r2.json >&2
              exit 1
            fi
          done

      - name: Provision DigitalOcean with Terraform
        id: terraform
        env:
          SSH_PUBLIC_KEY: ${{ secrets.DIGITALOCEAN_SSH_PUBLIC_KEY }}
          REGION: ${{ inputs.digitalocean_region }}
          ADMIN_CIDR: ${{ inputs.admin_cidr }}
          DOMAIN: ${{ inputs.domain }}
        run: |
          set -euo pipefail
          cd infrastructure/digitalocean
          terraform init -reconfigure \
            -backend-config="bucket=jalwa-terraform-state" \
            -backend-config="key=production/digitalocean.tfstate" \
            -backend-config="region=auto" \
            -backend-config="skip_credentials_validation=true" \
            -backend-config="skip_metadata_api_check=true" \
            -backend-config="skip_region_validation=true" \
            -backend-config="skip_requesting_account_id=true" \
            -backend-config="skip_s3_checksum=true" \
            -backend-config="use_path_style=true" \
            -backend-config="endpoints={s3=\"https://$CLOUDFLARE_ACCOUNT_ID.r2.cloudflarestorage.com\"}"
          terraform apply -auto-approve \
            -var="digitalocean_token=$DIGITALOCEAN_TOKEN" \
            -var="region=$REGION" \
            -var="ssh_public_key=$SSH_PUBLIC_KEY" \
            -var="admin_cidrs=[\"$ADMIN_CIDR\"]" \
            -var="domain=$DOMAIN"
          host=$(terraform output -raw ipv4_address)
          echo "host=$host" >> "$GITHUB_OUTPUT"

      - name: Configure Cloudflare DNS
        env:
          HOST: ${{ steps.terraform.outputs.host }}
          DOMAIN: ${{ inputs.domain }}
        run: |
          set -euo pipefail
          upsert_record() {
            local name="$1"
            local existing
            existing=$(curl -fsS "https://api.cloudflare.com/client/v4/zones/$CLOUDFLARE_ZONE_ID/dns_records?type=A&name=$name" \
              -H "Authorization: Bearer $CLOUDFLARE_API_TOKEN" | jq -r '.result[0].id // empty')
            body=$(jq -nc --arg type A --arg name "$name" --arg content "$HOST" '{type:$type,name:$name,content:$content,ttl:1,proxied:true}')
            if [[ -n "$existing" ]]; then
              curl -fsS -X PUT "https://api.cloudflare.com/client/v4/zones/$CLOUDFLARE_ZONE_ID/dns_records/$existing" \
                -H "Authorization: Bearer $CLOUDFLARE_API_TOKEN" -H 'Content-Type: application/json' --data "$body" >/dev/null
            else
              curl -fsS -X POST "https://api.cloudflare.com/client/v4/zones/$CLOUDFLARE_ZONE_ID/dns_records" \
                -H "Authorization: Bearer $CLOUDFLARE_API_TOKEN" -H 'Content-Type: application/json' --data "$body" >/dev/null
            fi
          }
          upsert_record "$DOMAIN"
          upsert_record "www.$DOMAIN"

      - name: Create or discover Supabase project
        id: supabase
        env:
          REGION: ${{ inputs.supabase_region }}
        run: |
          set -euo pipefail
          project_name="jalwa-production"
          ref=$(supabase projects list --output json | jq -r --arg name "$project_name" '.[] | select(.name==$name) | .ref' | head -n1)
          if [[ -z "$ref" ]]; then
            supabase projects create "$project_name" --org-id "$SUPABASE_ORG_ID" --db-password "$SUPABASE_DB_PASSWORD" --region "$REGION"
            for attempt in $(seq 1 30); do
              ref=$(supabase projects list --output json | jq -r --arg name "$project_name" '.[] | select(.name==$name) | .ref' | head -n1)
              [[ -n "$ref" ]] && break
              sleep 10
            done
          fi
          test -n "$ref" || { echo 'Supabase project was not found after creation.' >&2; exit 1; }
          echo "ref=$ref" >> "$GITHUB_OUTPUT"

      - name: Persist non-secret deployment outputs
        env:
          HOST: ${{ steps.terraform.outputs.host }}
          SUPABASE_REF: ${{ steps.supabase.outputs.ref }}
        run: |
          set -euo pipefail
          gh variable set PRODUCTION_HOST --env production --body "$HOST"
          gh variable set PRODUCTION_USER --env production --body "jalwa"
          gh variable set SUPABASE_PROJECT_REF --env production --body "$SUPABASE_REF"
          gh variable set NEXT_PUBLIC_SUPABASE_URL --env production --body "https://$SUPABASE_REF.supabase.co"
          gh variable set PRODUCTION_DOMAIN --env production --body "${{ inputs.domain }}"
          {
            echo '### Platform bootstrap complete'
            echo "- DigitalOcean host: $HOST"
            echo "- Supabase project ref: $SUPABASE_REF"
            echo '- R2 buckets and Cloudflare DNS configured'
          } >> "$GITHUB_STEP_SUMMARY"
